/**
 * 默认配置
 */
export const defaultOptions = {
  width: 100,
  rotate: -20,
  zIndex: 1,
  fontStyle: {
    color: '#000',
    fontFamily: 'PingFang SC',
    fontSize: 14,
    fontWeight: 400,
  },
  gap: [100, 100],
  offset: [0, 0],
  getContainer: () => document.body,
}
